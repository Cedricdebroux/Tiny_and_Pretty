{{ form_start(form3) }}
{{ form_row(form3.bebe) }}
<h4>Bébé</h4>
{{ form_row(form3.babies) }}


<h4>Prix</h4>
<div class="row">
    <div class="col-md-6">
        {{ form_row(form3.min) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form3.max) }}
    </div>
</div>

<button type="submit" class="btn btn-primary w-100">Filtrer</button>
{{ form_end(form3) }}
